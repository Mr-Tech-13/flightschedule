{
  "name": "flightschedule",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env server/index.js",
    "dev": "node --env-file-if-exists=.env --watch server/index.js",
    "build": "node --check server/index.js && node --check server/auth.js && node --check server/db.js && node --check server/employee-schedule-parser.js && node --check server/flight-parser.js && node --check server/image-schedule-parser.js && node --check server/scheduler.js && node --check server/scheduler-eligibility.js && node --check public/app.js",
    "lint": "eslint .",
    "test": "node --test",
    "check": "npm run lint && npm run build && npm test"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.3.14",
    "bcryptjs": "^3.0.2",
    "cookie-parser": "^1.4.7",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "multer": "^2.0.2",
    "read-excel-file": "^9.3.10",
    "tesseract.js": "^6.0.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.10.0"
  }
}
